抽象工厂模式就是创建多个工厂类，这样一旦需要增加新的功能，直接增加新的工厂类就可以了，不需要修改之前的代码。
The abstract factory pattern is to create multiple factory class, 
so if need to add new features, new factory class can directly, 
and don't need to modify the code before.